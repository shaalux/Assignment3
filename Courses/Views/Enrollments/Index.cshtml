@model IEnumerable<Courses.Models.Enrollment>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.AdmissionTermCode)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Course.CourseName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Major.Id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Grade)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.EnrolledIndicator)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PaymentIndicator)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Admission.TermCode)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Course.CourseName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Major.Id)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Grade)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.EnrolledIndicator)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PaymentIndicator)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.StudentsId }) |
            @Html.ActionLink("Details", "Details", new { id=item.StudentsId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.StudentsId }) 
          
        </td>
    </tr>
}

</table>
